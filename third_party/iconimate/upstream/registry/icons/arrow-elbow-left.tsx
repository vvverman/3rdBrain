"use client";

import { makeDrawElbow } from "./_draw-elbow";

// DRAW — a "turn" arrow: tail drops from the top-right to a vertex, then the head
// snaps back up-left.
export const ArrowElbowLeftIcon = makeDrawElbow("M224,92L136,180L34,90");
