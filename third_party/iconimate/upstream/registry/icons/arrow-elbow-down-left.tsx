"use client";

import { makeDrawElbow } from "./_draw-elbow";

// DRAW — vertical shaft drops from the top-right, elbows, and the head snaps left.
export const ArrowElbowDownLeftIcon = makeDrawElbow("M192,40L192,176L40,176");
