"use client";

import { makeDrawElbow } from "./_draw-elbow";

// DRAW — horizontal shaft runs in from the left, elbows, and the head snaps down.
export const ArrowElbowRightDownIcon = makeDrawElbow("M32,72L176,72L176,212");
