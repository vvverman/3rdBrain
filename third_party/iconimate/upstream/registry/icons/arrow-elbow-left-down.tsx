"use client";

import { makeDrawElbow } from "./_draw-elbow";

// DRAW — horizontal shaft runs in from the right, elbows, and the head snaps down.
export const ArrowElbowLeftDownIcon = makeDrawElbow("M232,72L88,72L88,216");
