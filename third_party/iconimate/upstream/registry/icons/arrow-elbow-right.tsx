"use client";

import { makeDrawElbow } from "./_draw-elbow";

// DRAW — a "turn" arrow: tail drops from the top-left to a vertex, then the head
// snaps back up-right.
export const ArrowElbowRightIcon = makeDrawElbow("M32,92L120,180L222,90");
