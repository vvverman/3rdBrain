# 3rdBrain

Локальный голосовой блокнот для iPhone. Нативный SwiftUI, Liquid Glass и системные модели Apple. Без сторонних библиотек приложения, синхронизации iCloud, собственного сервера и ключей API.

**Открыл → говоришь → завершил → выбрал проект → новая заметка или добавление в конец существующей.**

## Первый запуск и проверка русского

**Сначала пробуем всё на Apple.** Whisper и альтернативная текстовая LLM не включены в текущую сборку. При проблемах с русским сначала фиксируем результат на конкретном iPhone, затем меняем только нужный движок.

В настройках приложения доступны:

- отдельная диагностика SpeechTranscriber, DictationTranscriber и Foundation Models;
- «Подготовить русское распознавание» — подтверждённая загрузка системных речевых ресурсов Apple;
- «Проверить оформление на примере» — проверка существующего обработчика Foundation Models на русском тексте, если язык и модель доступны;
- отправка диагностики и результата теста через системное меню.

Пошаговый сценарий: **[docs/APPLE_DEVICE_CHECK.md](docs/APPLE_DEVICE_CHECK.md)**. Исходное ТЗ: [docs/SPEC.md](docs/SPEC.md). Решение по моделям: [docs/RUSSIAN.md](docs/RUSSIAN.md). Общий чек-лист: [docs/TESTING.md](docs/TESTING.md); фактически выполненные проверки: [docs/VERIFICATION.md](docs/VERIFICATION.md).

## Что включено в первый проход

Автозапись после явного согласия и разрешения микрофона; постоянная нижняя панель, пауза и завершение; навигация во время записи. Проекты с полем «Что сюда складывать», закреплением и ручным порядком закреплённых. Полный список проектов с локальным ИИ-ранжированием при доступной модели; ручной выбор заметки. Отдельные источники каждого добавления, полная транскрипция, компактное аудио, таймкоды и ускоренное воспроизведение. Входящие, редактирование, поиск по входящим, экспорт текста/аудио через системное меню.

## Системные модели Apple

Распознавание выбирает поддерживаемый локальный модуль `SpeechTranscriber` или `DictationTranscriber`. Сначала используются уже установленные ресурсы; при равных условиях предпочтителен SpeechTranscriber. Если модуль не распознал запись, пробуется другой доступный модуль Apple. Отмена не запускает следующий модуль. При отсутствии ресурсов предлагается их подготовка с подтверждением. Само распознавание выполняется на устройстве.

Наличие iOS 26 не гарантирует Apple Intelligence и поддержку языка в Foundation Models. Приложение проверяет `SystemLanguageModel.availability` и `supportsLocale` на конкретном устройстве. Не используем статический запрет русского по списку на сайте и не обходим ответ системного API.

Если LLM недоступна, сохраняется полный транскрипт без ИИ-оформления. Проекты показаны с закреплёнными сверху и обычной сортировкой остальных. Ручной выбор и редактирование работают. Если недоступно и распознавание, аудио остаётся во входящих. Скрытого перехода на другую модель или облако нет.

## Запуск на iPhone

1. Открыть **ThirdBrain.xcodeproj** в **Xcode 26 или новее** на Mac.
2. Выбрать схему **ThirdBrain**, затем подключённый iPhone с **iOS 26+**.
3. В **Signing & Capabilities** выбрать свою **Personal Team**. При конфликте заменить Bundle Identifier на уникальный.
4. Включить режим разработчика на iPhone, если Xcode/телефон попросит. Нажать запуск.
5. Прочитать экран согласия и разрешить микрофон. Создать проект и заполнить его инструкцию.
6. Выполнить проверки из раздела «Первый запуск и проверка русского» выше.

В проекте нет CloudKit, iCloud, App Groups или платных облачных возможностей. Бесплатная подпись Personal Team имеет ограничения срока действия; при истечении подписи приложение нужно подписать заново через Xcode. Не удаляйте приложение для переподписи без сохранения важных данных.

## Проверки

```bash
# Алгоритмы без iOS SDK; работает также на Linux со Swift 6.
swift test

# На Mac: основные тесты, сборка iOS и тесты SwiftData в симуляторе.
bash scripts/check.sh
```

Сборка симулятора не доказывает работоспособность микрофона и языковых моделей на физическом iPhone. Подробный ручной чек-лист и известные ограничения — в `docs/TESTING.md`.

## Хранение и поведение

SwiftData хранит проекты, заметки и записи. Аудио находится в постоянном каталоге Application Support/3rdBrain/Audio. Оригинал — моно CAF/PCM 16 кГц, около 115 МБ на час; компактная версия — M4A. Оригинал не меняется и не удаляется автоматически. Сокращение пауз использует консервативную оценку громкости, а не полноценный речевой детектор: тихую речь всегда следует сверять с оригиналом.

Транскрипция выполняется по оригиналу. Для компактного аудио хранится таблица соответствия времени. Скорость меняется в проигрывателе без необратимого ускорения файла. Прослушивание автоматически ставит микрофон на паузу.

Обработка идёт по одной записи, готовые этапы сохраняются отдельно. При уходе в фон обработка отменяется с сохранением источника; повторный запуск не теряет готовый транскрипт. Активная аудиозапись использует штатный фоновый аудиорежим. Звонки и отключение аудиоустройства не снимают паузу автоматически.

Каждая распределённая запись хранит идентификатор заметки: повторное сохранение не добавляет текст дважды. Удаление заметки возвращает её источники во входящие. Удаление входящей записи с аудио требует подтверждения. Удаление приложения может привести к потере всех локальных данных; системные резервные копии зависят от настроек iOS.

## Документация Apple

- [SpeechAnalyzer и системные модули](https://developer.apple.com/videos/play/wwdc2025/277/)
- [SystemLanguageModel](https://developer.apple.com/documentation/foundationmodels/systemlanguagemodel)
- [Foundation Models и языки](https://developer.apple.com/documentation/foundationmodels/supporting-languages-and-locales-with-foundation-models)
- [Поддержка Apple Intelligence](https://support.apple.com/121115)
- [Liquid Glass в SwiftUI](https://developer.apple.com/documentation/SwiftUI/Applying-Liquid-Glass-to-custom-views)
- [Personal Team](https://developer.apple.com/support/compare-memberships/)
