name: Проверка iOS
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check:
    name: Xcode и тесты
    runs-on: macos-26
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Выбрать Xcode 26
        shell: bash
        run: |
          set -euo pipefail
          XCODE=$(python3 - <<'PY'
          import glob, re
          paths = glob.glob('/Applications/Xcode_26*.app')
          stable = [p for p in paths if 'beta' not in p.lower()]
          if not stable:
              raise SystemExit('Не найден стабильный Xcode 26')
          print(sorted(stable, key=lambda p: tuple(map(int, re.findall(r'\d+', p))))[-1])
          PY
          )
          sudo xcode-select --switch "$XCODE/Contents/Developer"
          xcodebuild -version
          swift --version
      - name: Ограничения MVP и конфигурация
        run: python3 scripts/check_contract.py
      - name: Тесты алгоритмов
        timeout-minutes: 3
        shell: bash
        run: |
          set -o pipefail
          swift test 2>&1 | tee core.log
          swift test -c release 2>&1 | tee core-release.log
      - name: Сборка приложения и тестового пакета
        timeout-minutes: 12
        shell: bash
        run: |
          set -o pipefail
          xcodebuild -project ThirdBrain.xcodeproj -scheme ThirdBrain -configuration Debug \
            -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath DerivedData CODE_SIGNING_ALLOWED=NO build-for-testing 2>&1 | tee build.log
      - name: Тесты SwiftData, аудио, обработки и интерфейса
        timeout-minutes: 10
        shell: bash
        run: |
          set -euo pipefail
          DEVICE=$(xcrun simctl list devices available --json | python3 -c '
          import json,sys
          for runtime, devices in json.load(sys.stdin)["devices"].items():
              if "iOS-26" not in runtime: continue
              for device in devices:
                  if device.get("isAvailable") and device["name"].startswith("iPhone"):
                      print(device["udid"]); sys.exit(0)
          sys.exit("Не найден доступный симулятор iPhone с iOS 26")
          ')
          xcodebuild -project ThirdBrain.xcodeproj -scheme ThirdBrain -configuration Debug \
            -destination "platform=iOS Simulator,id=$DEVICE" -destination-timeout 60 \
            -parallel-testing-enabled NO -test-timeouts-enabled YES \
            -maximum-test-execution-time-allowance 90 \
            -derivedDataPath DerivedData -resultBundlePath TestResults.xcresult \
            CODE_SIGNING_ALLOWED=NO test-without-building 2>&1 | tee simulator.log
      - name: Release-сборка для физического iPhone без подписи
        timeout-minutes: 8
        shell: bash
        run: |
          set -o pipefail
          xcodebuild -project ThirdBrain.xcodeproj -scheme ThirdBrain -configuration Release \
            -sdk iphoneos -destination 'generic/platform=iOS' -derivedDataPath DerivedDataDevice \
            CODE_SIGNING_ALLOWED=NO build 2>&1 | tee device-release.log
      - name: Скриншоты UI-тестов
        if: always()
        shell: bash
        run: |
          if [ -d TestResults.xcresult ]; then
            xcrun xcresulttool export attachments --path TestResults.xcresult --output-path Screenshots
          fi
      - name: Сохранить исходники проверяемого коммита
        if: always()
        run: git archive --format=zip --output=source.zip HEAD
      - name: Журнал и результаты
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-check-${{ github.sha }}
          path: |
            core.log
            core-release.log
            device-release.log
            Screenshots
            build.log
            simulator.log
            TestResults.xcresult
            source.zip
          retention-days: 7
