# Фактически выполненные проверки

## Проверенная версия

Проверен код коммита `41f425257ec063b86f0faf72a93a96db2473814a`. Реализация приложения внесена в `59d89448e3225fa14c637dff83bed5950dcd9b4b`; следующие изменения до этой проверки касались настройки CI и документации, а не логики приложения.

7 сентября 2026 года [GitHub Actions №1](https://github.com/vvverman/3rdBrain/actions/runs/34159935477) завершился **успешно**. Проверены сборка приложения и тестового пакета, алгоритмы и хранение SwiftData.

## Xcode и симулятор

Среда проверки: macOS, Xcode 26.4, Swift 6.2.4, симулятор iPhone 17 Pro с iOS 26.4.

- `xcodebuild build-for-testing`: **TEST BUILD SUCCEEDED**.
- `xcodebuild test-without-building`: **TEST EXECUTE SUCCEEDED**.
- **14 тестов алгоритмов прошли**.
- **3 теста SwiftData прошли**, ошибок и неожиданных сбоев нет.

Итого: **17 успешных тестов**. Это реальный результат запуска, а не только наличие файлов тестов.

Тесты SwiftData:

1. `testAppendIsIdempotentAndKeepsSources`: повторное сохранение не дублирует добавление, старый текст и оба источника остаются.
2. `testWrongProjectDoesNotModifyNote`: нельзя добавить запись в заметку другого проекта; её текст не меняется.
3. `testDeletingNoteReturnsSourcesToInbox`: удаление заметки возвращает её источники во входящие, не удаляя записи.

Журнал сборки, результаты Xcode и архив исходников проверенного коммита сохранены в артефактах указанного запуска GitHub Actions.

## Дополнительная проверка алгоритмов

Также выполнен `swift test` в Linux, Swift 6.2.1: **14 тестов прошли, ошибок нет**.

Перед этим запуском проверено побайтовое совпадение локальных файлов с GitHub через Git blob SHA:

- `Package.swift`: `3c6a42e3d2cd8403ceeb3b336b3369d9ba1f65c7`.
- `ThirdBrain/Core/BrainCore.swift`: `238804247c5e1a318c43d31fec8c93dc38dd0c74`.
- `CoreTests/BrainCoreTests.swift`: `b90381b76a60eea4b7cc08a6463981cd3bae62d8`.

Проверены сохранение русского текста и его конца при делении на фрагменты, длинные слова и пустой текст, сокращение длинных пауз, сохранение полностью тихого источника, преобразование таймкодов, закрепления и их ручной порядок, наличие всех проектов в списке, добавление нового текста без изменения старого.

## Физический iPhone

**Пока не проверен.** Микрофон, блокировка экрана, аудиопрерывания, доступность и качество русского распознавания, поддержка языка в Foundation Models, установка с Personal Team и энергопотребление требуют реального устройства. Чек-лист — [TESTING.md](TESTING.md).

Успешная сборка не подтверждает поддержку русского языка локальной LLM. Приложение проверяет доступность и язык модели отдельно; при отсутствии поддержки сохраняет транскрипт и предоставляет ручной выбор проекта без скрытой облачной обработки.
