# Фактически выполненные проверки

## Whisper — 8 сентября 2026

Проверен код коммита `8697b014c6ad676a06589588db209159162193fa`. Последующие изменения README, RUSSIAN.md и этого отчёта являются только документацией и не меняют проверенный код.

[GitHub Actions №5](https://github.com/vvverman/3rdBrain/actions/runs/34187876288), задание `101939766985`, завершилось **успешно**. Использованы Xcode 26.6 и симулятор iPhone 17 Pro с iOS 26.4.1. Зависимость WhisperKit закреплена на Argmax OSS Swift 1.1.0.

- `swift test`: **23 теста прошли** — 14 прежних тестов алгоритмов и 9 новых тестов маршрутизации распознавания.
- `xcodebuild build-for-testing`: **TEST BUILD SUCCEEDED**; собраны приложение, WhisperKit и тестовый пакет.
- `xcodebuild test-without-building`: **TEST EXECUTE SUCCEEDED**; **8 тестов прошли**, из них 3 теста SwiftData и 5 тестов интеграционного кода Whisper.

Итого: **31 успешный тест, 0 проваленных**. Это исполненные тесты, не только созданные файлы.

Новые проверки подтверждают выбор Apple или Whisper, переход к резервному движку при ошибке, отсутствие такого перехода после отмены, соответствие локалей ru-RU/ru_RU, настройки ru/transcribe без перевода на английский, сохранение текста всех результатов и конца длинной записи, обработку неверных таймкодов, отсутствие загрузки без согласия, распознавание неполной установки и сохранность аудио при удалении модели.

Артефакт запуска: `10041220921`, имя `ios-check-8697b014c6ad676a06589588db209159162193fa`. Содержит core.log, build.log, simulator.log, TestResults.xcresult и source.zip. SHA-256 скачанного архива проверен: `fd2122edc15717eb1b22a4980477ac85aaed99efa93b617d0add093e11d729ac`. Все Swift-файлы и Xcode-проект в source.zip побайтово совпали с локальными проверяемыми исходниками.

В журнале первого запуска SwiftData были диагностические сообщения об отсутствующем каталоге Application Support. Затем Core Data сообщила об успешном восстановлении, и все тесты прошли. Поэтому утверждение «в журнале вообще нет ошибок» не делается.

Ранее в этом изменении исправлены два препятствия сборке: минимальные платформы Swift-пакета BrainCore и использование закрытого конструктора WhisperTokenizerWrapper. Вместо него применяется адаптер публичного протокола LocalWhisperTokenizer. Полная повторная проверка после этих исправлений — указанный успешный запуск №5.

Также выполнен `swift test` локально в Linux со Swift 6.2.1: **23 теста прошли**.

### Что этот запуск не проверяет

Веса Whisper в CI не скачивались, реальная речь моделью не распознавалась. Тесты проверяют интеграционный код, но не качество русского распознавания, фактическую загрузку весов на iPhone, работу всей модели в авиарежиме, скорость и энергопотребление. Это остаётся проверкой на физическом устройстве по [RUSSIAN.md](RUSSIAN.md).

Whisper не заменяет текстовую LLM. Эта проверка не подтверждает русское ИИ-оформление или ранжирование через Foundation Models. При отсутствии поддержки сохраняется транскрипт и остаётся ручной выбор проекта.

## Предыдущая проверенная версия — 7 сентября 2026

Проверен код коммита `41f425257ec063b86f0faf72a93a96db2473814a`. Реализация первого прохода внесена в `59d89448e3225fa14c637dff83bed5950dcd9b4b`.

[GitHub Actions №1](https://github.com/vvverman/3rdBrain/actions/runs/34159935477) завершился успешно. Проверены сборка приложения и тестового пакета, 14 тестов алгоритмов и 3 теста SwiftData: всего 17 успешных тестов. Это результат версии до добавления Whisper.

Тесты SwiftData:

1. `testAppendIsIdempotentAndKeepsSources`: повторное сохранение не дублирует добавление, старый текст и оба источника остаются.
2. `testWrongProjectDoesNotModifyNote`: нельзя добавить запись в заметку другого проекта; её текст не меняется.
3. `testDeletingNoteReturnsSourcesToInbox`: удаление заметки возвращает её источники во входящие, не удаляя записи.

Также выполнен `swift test` в Linux, Swift 6.2.1: 14 тестов прошли. Перед этим запуском проверено побайтовое совпадение локальных файлов с GitHub через Git blob SHA:

- `Package.swift`: `3c6a42e3d2cd8403ceeb3b336b3369d9ba1f65c7`.
- `ThirdBrain/Core/BrainCore.swift`: `238804247c5e1a318c43d31fec8c93dc38dd0c74`.
- `CoreTests/BrainCoreTests.swift`: `b90381b76a60eea4b7cc08a6463981cd3bae62d8`.

Проверены сохранение русского текста и его конца при делении на фрагменты, длинные слова и пустой текст, сокращение длинных пауз, сохранение полностью тихого источника, преобразование таймкодов, закрепления и их ручной порядок, наличие всех проектов в списке, добавление нового текста без изменения старого.

## Физический iPhone

**Пока не проверен.** Микрофон, блокировка экрана, аудиопрерывания, доступность и качество русского распознавания, поддержка языка в Foundation Models, установка с Personal Team и энергопотребление требуют реального устройства. Чек-листы — [TESTING.md](TESTING.md) и [RUSSIAN.md](RUSSIAN.md).
