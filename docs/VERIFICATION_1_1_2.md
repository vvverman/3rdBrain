# Проверки Kasha Test 1.1.2 — 10 сентября 2026

Программный коммит: **`ae2a6dbada0232cdb301a48d33234fa490b9dedc`**.

- macOS build/self-test: run 34522064350
- общий код и браузерный путь: run 34522064501
- установщик: artifact 10170130075

Оба финальных прогона завершились успешно.

## Что проверено

В macOS-артефакте найдено **93 уникальных Kotlin-теста**: 0 failures, 0 errors, 0 skipped. Проверены общее ядро, состояния Compose-интерфейса, файловое хранилище, обработка аудио и macOS-адаптеры.

Отдельный браузерный сценарий прошёл 11 групп действий реальными кликами по UI: Главная и постоянный плеер; первый проект; запись и навигация; редактирование текущего текста; сохранение в проект; **закрепление сохранённой заметки и встроенный редактор**; воспроизведение; конфликт записи/прослушивания; создание проекта из выбора; удаление; восемь языков и обе темы. `pageErrors = []`.

Большой блок настройки демонстрационных текстов удалён из экрана Настроек. Тестовая сборка по-прежнему явно обозначает, что ИИ имитируется, но пользователь не управляет внутренними тестовыми сценариями через обычные настройки.

## Заметки

Модель `Note` теперь хранит `pinned` и `pinOrder`. Закреплённые заметки показываются первыми внутри проекта. Закрепление сохраняется после ручного редактирования и после добавления следующего голосового фрагмента в конец заметки.

Сохранённая заметка имеет лёгкий встроенный текстовый редактор: название + многострочное тело, «Сохранить» и «Отмена». Сохранение обновляет саму заметку и не переписывает/не удаляет её аудиоисточники.

## macOS-пакет

Файл: **`Kasha-Test-1.1.2-macOS-arm64.dmg`**.

Размер: **142 719 380 байт**.
SHA-256: **`7c8113a0886767fc06b605e2cdbb01b34482c4ce2e9f7b9fa97e2d921ccf538c`**.

Нативная самопроверка выполнена на macOS 15.7.9 arm64. В пакет встроены Java и FFmpeg. Веса Whisper/Qwen отсутствуют, внешняя сеть при self-test запрещена. Аудиообработка настоящая; распознавание и LLM в этой тестовой поставке имитируются.

Сборка ad-hoc, без Developer ID и notarization. Физический микрофон и акустическое воспроизведение на пользовательском Mac автоматикой не проверены.
